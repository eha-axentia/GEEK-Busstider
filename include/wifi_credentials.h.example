#pragma once

// Copy this file to wifi_credentials.h and fill in your credentials.
// wifi_credentials.h is gitignored and will never be committed.

#define WIFI_SSID     "your_ssid_here"
#define WIFI_PASSWORD "your_password_here"
